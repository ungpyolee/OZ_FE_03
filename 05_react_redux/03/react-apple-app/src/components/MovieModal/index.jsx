import React from 'react'

const MovieModal = ({
    backgrop_path,
    title,
    overview,
    name,
    release_date,
    first_air_date,
    vote_average,
    setModalOpen
}) => {
  return (
    <div>MovieModal</div>
  )
}

export default MovieModal